import { Place } from "@/interfaces/place.interface";
import React, { FC } from "react";

interface BuyTicketScreenProps {
  places: Place[];
}

const BuyTicketScreen: FC<BuyTicketScreenProps> = ({ places }) => {
  return (
    <>
      
    </>
  );
}

export default BuyTicketScreen;